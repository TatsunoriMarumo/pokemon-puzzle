server:
  address: 0.0.0.0
  port: ${PORT:8080}

spring:
  application:
    name: backend
  messages:
    basename: messages

pokeapi:
  base-url: https://pokeapi.co/api/v2
  pokemon:
    limit: 100000
    offset: 0
    max-retry-count: 20

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
